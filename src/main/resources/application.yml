server:
  port: 8080

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/timetracker
    username: root
    password: your_password
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
jwt:
  secret: my-super-secure-key-123456789